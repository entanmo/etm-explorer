
const user = {
  state: {
    
    
  },
  mutations: {   //更改store中的状态是提交mutation (mutation必须同步执行)
    
    
  },
  actions: {     //action提交的是mutation,而不是直接变更状态,action可以包含任意异步操作。
    
    
  }
}
export default user
